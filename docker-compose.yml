version: '3.9'

services:
    # NGINX container
    nginx:
        container_name: ${APP_NAME}-nginx
        build:
            context: .docker/nginx
            dockerfile: Dockerfile
        ports:
            - 88:80
        volumes:
            - .:/var/www/${APP_NAME}
            - .docker/nginx/logs:/var/log/nginx
        links:
            - php
        networks:
            - ${APP_NAME}-network

    # PHP FastCGI Process Manager container
    php:
        container_name: ${APP_NAME}-php
        build:
            context: .
            dockerfile: .docker/php/Dockerfile
            args:
              - APP_ENV=${APP_ENV}
              - APP_NAME=${APP_NAME}
        volumes:
            - .:/var/www/${APP_NAME}
            - .docker/php/php.ini:/usr/etc/php/php.ini
        networks:
            - ${APP_NAME}-network

networks:
  RMS-network:
      driver: bridge